{
  "name": "{{basename}}",
  "version": "0.0.1",
  "description": "{{description}}",
  "main": "index.js",
  "scripts": {
    "start": "node index",
    "start:watch": "nodemon -e js,json -x 'npm start --silent'",
    "lint": "semistandard | snazzy",
    "test": "npm run lint",
    "test:watch": "nodemon -e js,json -x 'npm run test --silent'"
  },
  "author": "Tak Tran <contact@tutaktran.com>",
  "license": "MIT",
  "devDependencies": {
    "semistandard": "8.0.0",
    "snazzy": "5.0.0"
  }
}
