{
  "name": "{{basename}}",
  "version": "0.0.1",
  "description": "{{description}}",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/taktran/{{basename}}.git"
  },
  "scripts": {
    "start": "node index",
    "start:watch": "nodemon -e js,json -x 'npm start --silent'",
    "test": "npm run lint",
    "test:watch": "nodemon -e js,json -x 'npm run test --silent'"
  },
  "author": "Tak Tran <contact@tutaktran.com>",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.15.3"
  }
}
